package model

import org.apache.spark.ml.{Pipeline, PipelineModel}

object ModelPipeline {
  def apply(inputCol: String): PipelineModel = {
    // TODO load a model, join with cleaning pipelines, create a full pipeline (raw data -> prediction)
    null
  }
}
